{{ form_start(form) }}

    {# {{ form_errors(form) }} #}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.techno) }}
    {{ form_row(form.image) }}

    <p class="mt-2">* Champ obligatoire</p>
    <button class="btn btn-primary m-1">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
