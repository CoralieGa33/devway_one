import React from 'react';

import './form.scss';

const Form = () => (
    <form className="form">
        <input
        className="form-input"
        type="text"
        placeholder="Ajouter une tâche"
        />
    </form>
);

export default Form;
