import { connect } from 'react-redux';

import Realisations from '/assets/js/realisations/components/Realisations';

import { } from '/assets/js/realisations/actions/realisationsActions';

const mapStateToProps = (state) => ({

});

const mapDispatchToProps = (dispatch) => ({

});

const RealisationsContainer = connect(
    mapStateToProps,
    mapDispatchToProps,
)(Realisations);

export default RealisationsContainer;