import { } from '/assets/js/realisations/actions/realisationsActions';

const initialState = {
    // ici l'état initial
};

const realisationsReducer = (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default realisationsReducer;